services:
  - type: web
    name: ai-image-studio
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: HF_API_KEY
        sync: false
      - key: HORDE_APIKEY
        sync: false
    plan: free
